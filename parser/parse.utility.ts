import type { Inline } from "./types.js";

export function parseInline(input: string): Inline[] {
  // Look for italics, bolds, commands, glossary references, etc.
  const inlineNodes: Inline[] = [];
  // TODO

  return inlineNodes;
}
